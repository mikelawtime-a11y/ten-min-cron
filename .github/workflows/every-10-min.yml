name: Weekly

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello + timestamp
        run: |
          echo "Hello from scheduled workflow!"
          date -u
